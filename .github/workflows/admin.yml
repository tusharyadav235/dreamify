# ═══════════════════════════════════════════════════════════════════
#  DREAMIFY — ADMIN PANEL CI/CD PIPELINE (GHCR + EC2)
# ═══════════════════════════════════════════════════════════════════

name: Admin Panel CI/CD

on:
  push:
    branches: [main]
    paths:
      - "admin-panel/**"
      - ".github/workflows/admin.yml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  # IMAGE_NAME must match your docker-compose.yml image field
  IMAGE_NAME: tusharyadav235/dreamify-admin

jobs:
  build:
    name: 🐳 Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🏗️ Build and push
        uses: docker/build-push-action@v5
        with:
          context: admin-panel
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: 🚀 Deploy to EC2
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd ~/dreamify
            
            echo "🌿 1. Syncing docker-compose and configs..."
            git fetch --all
            git reset --hard origin/main
            
            echo "🔑 2. Authenticating with GHCR..."
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            echo "🐳 3. Pulling fresh Admin image from GHCR..."
            docker compose pull admin
            
            echo "🚀 4. Recreating Admin container..."
            # --no-deps ensures only the admin container restarts
            # --force-recreate ensures it uses the new image layers
            docker compose up -d --no-deps --force-recreate admin
            
            echo "🧹 5. Cleaning up old image layers..."
            docker image prune -f
            
            echo "🎉 [SUCCESS] Admin Panel is updated and LIVE!"
